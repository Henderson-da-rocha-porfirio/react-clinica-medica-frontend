import React from "react";

class GeradorGrafico extends React.Component {        // Todo component React herda da classe React.Component e implementa o método Render.
    render(){                               // Método render é aquele responsável pelo que aparece no Browser e retornará o que estiver dentro dos parênteses.
                                            // Retornando um simples elemento de div e é preciso externar este component. Que exporta JSx.
        return(<div>                        
    
    </div>)
    }
}
    export default GeradorGrafico;                    // Exportando essa div. Daí agora poderemos utilizar em outros componentes.

                         